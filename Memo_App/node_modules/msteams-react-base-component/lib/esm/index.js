// Copyright (c) Wictor Wilén. All rights reserved.
// Licensed under the MIT license.
// SPDX-License-Identifier: MIT
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import * as React from "react";
import { render } from "react-dom";
import { teamsTheme, teamsDarkTheme, teamsHighContrastTheme } from "@fluentui/react-northstar";
import * as microsoftTeams from "@microsoft/teams-js";
/**
 * Base implementation of the React based interface for the Microsoft Teams app
 */
var TeamsBaseComponent = /** @class */ (function (_super) {
    __extends(TeamsBaseComponent, _super);
    function TeamsBaseComponent() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        /**
         * Returns true if hosted in Teams
         * @param timeout timeout in milliseconds, default = 1000
         * @returns a `Promise<boolean>`
         */
        _this.inTeams = function (timeout) {
            if (timeout === void 0) { timeout = 1000; }
            return new Promise(function (resolve, reject) {
                try {
                    microsoftTeams.initialize(function () {
                        resolve(true);
                    });
                    setTimeout(function () {
                        resolve(false);
                    }, timeout);
                }
                catch (e) {
                    reject(e);
                }
            });
        };
        /**
         * Updates the theme
         */
        _this.updateTheme = function (themeStr) {
            var theme;
            switch (themeStr) {
                case "dark":
                    theme = teamsDarkTheme;
                    break;
                case "contrast":
                    theme = teamsHighContrastTheme;
                    break;
                case "default":
                default:
                    theme = teamsTheme;
            }
            _this.setState({ theme: theme });
        };
        /**
         * Returns the value of a query variable
         */
        _this.getQueryVariable = function (variable) {
            var query = window.location.search.substring(1);
            var vars = query.split("&");
            for (var _i = 0, vars_1 = vars; _i < vars_1.length; _i++) {
                var varPairs = vars_1[_i];
                var pair = varPairs.split("=");
                if (decodeURIComponent(pair[0]) === variable) {
                    return decodeURIComponent(pair[1]);
                }
            }
            return undefined;
        };
        return _this;
    }
    /**
     * Static method to render the component
     * @param element DOM element to render the control in
     * @param props Properties
     */
    TeamsBaseComponent.render = function (element, props) {
        return render(React.createElement(this, props), element);
    };
    return TeamsBaseComponent;
}(React.Component));
export default TeamsBaseComponent;
